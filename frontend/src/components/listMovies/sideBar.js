import styled from "styled-components";

export default function SideBar() {
  return <Wrappler>Em breve, filtros de pesquisa para essa página</Wrappler>;
}

const Wrappler = styled.div`
  width: 19%;
  height: 200px;
  background: rgb(255, 255, 255);
  margin: 40px 0 0 0;

  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 0 10px;
`;
